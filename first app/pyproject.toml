[project]
name = "first-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NimishMishra", email = "NimishMishra@PWLAP7483" }
]
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.3.16",
    "langchain-huggingface==1.2",
    "langchain-ollama>=1.1.0",
    "langchain-openai>=1.6.0",
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    "tzdata>=2026.3",
]

[project.scripts]
first-app = "first_app:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
